import 'package:flutter/material.dart';

class RequiredUpdateAppScreen extends StatelessWidget {
  const RequiredUpdateAppScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("RequiredUpdateAppScreen")),
    );
  }
}
